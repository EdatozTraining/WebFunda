//Declare a class (Person)
//name is a property

class Person {
    constructor(fn, ln) {
      this.first_name = fn;
      this.last_name = ln;
    }
}
// class Student extends Person {
//       console.log(`${this.name} also has a name.`);
//     }

  

//   const p1 = new Person("Vishnu", "Suruboyina");
//   console.log(p1.name + " is in " + p1.colour + " colour ")